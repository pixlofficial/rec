package dev.pixl.recorder.ui.theme

import androidx.compose.ui.graphics.Color

// Canvas & Surfaces (Deep Obsidian Black Palette)
val ObsidianCanvas = Color(0xFF08080C)
val SurfaceCard = Color(0xFF101018)
val SurfaceElevated = Color(0xFF161622)
val SurfaceRaised = Color(0xFF20202E)

// High-Contrast Borders & Hard Shadows
val BorderStark = Color(0xFF262636)
val BorderHighlight = Color(0xFFFFFFFF)
val ShadowSolid = Color(0xFF000000)

// Primary Brand & Action Accents (Red & White)
val HyperCrimson = Color(0xFFFF0033) // Electric Neon Red: Recording, Action, Stop, Active Pill
val TextPrimary = Color(0xFFFFFFFF)  // Pure Crisp White: Hero typography, Data, Primary Highlights
val TextSecondary = Color(0xFF8A8D9F) // Subtitles, Units, Secondary Specs
val TextMuted = Color(0xFF525464)     // Inactive Hints & Placeholders
val TextInverse = Color(0xFF08080C)

// Telemetry & Hardware Stats Accents (Lime & Amber)
val ToxicLime = Color(0xFF00FF66)   // Display Refresh Rate (FPS / HZ), Hardware SoC, Safe VU Levels
val CyberYellow = Color(0xFFFFE600) // Storage Telemetry (FREE GB), Estimated Rate, Warnings

// Auxiliary Highlights
val HyperCyan = Color(0xFF00E5FF)
val ElectricPurple = Color(0xFF9D4EDD)
